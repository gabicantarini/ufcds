#include <stdio.h>
#include <string.h>

int main()
{
    int littleMatrix[10];
    littleMatrix[0]=47;
    littleMatrix[1]=33;
    
    printf("The little indice Matrix %d is %d",0,littleMatrix[0] );
    
    return 0;
}